spring.application.name=hr-api-gateway-zuul

#Config Server
spring.cloud.config.uri=http://localhost:8888

# Allow actuator to update configuration in runtime
management.endpoints.web.exposure.include=*

